# Core ML Frameworks
torch>=2.3.0
torchvision>=0.18.0
torchaudio>=2.3.0

# Transformers & Model Loading
transformers>=4.57.0  # For Gemma/Qwen/LLaVA. InternVL needs ==4.48.3 (see requirements_internvl.txt)
accelerate>=0.30.0
peft>=0.10.0
trl>=0.8.0
bitsandbytes>=0.43.1

# Vision & Image Processing
pillow>=10.0.0
timm
torchvision>=0.18.0

# Retrieval
sentence-transformers>=2.7.0
chromadb>=0.5.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0,<2.0.0  # FAISS requires NumPy < 2.0
scikit-learn>=1.3.0
scipy>=1.11.0

# Utilities
tqdm
pyyaml>=6.0.0
einops
ninja
python-dotenv>=1.0.0

# Evaluation API baselines (optional; for GPT-4o, Gemini, Claude comparisons)
openai>=1.0.0
google-generativeai>=0.8.0
anthropic>=0.40.0

# Monitoring
wandb>=0.16.0

# Datasets
datasets

# Note: faiss-gpu and flash-attn need to be installed separately:
# - faiss-gpu: conda install -c pytorch faiss-gpu
# - flash-attn: pip install flash-attn --no-build-isolation (requires CUDA toolkit)
#
# Note: InternVL 2.5 requires a SEPARATE environment with transformers==4.48.3.
# See requirements_internvl.txt and CONTAINER_AND_ENVIRONMENT_GUIDE.md.
